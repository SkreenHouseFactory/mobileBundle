{% extends 'SkreenHouseFactoryMobileBundle::layout.html.twig' %}

{% block title %}A la Télé ce soir{% endblock %}

{% block content %}

<h3>Ce soir à la Télé</h3>
{% for channel in channels %}

<ul id="channel-{{ channel.id }}" data-role="listview" data-divider-theme="a" data-inset="true">
  <li data-role="list-divider" role="heading">
    <img src="{{ channel.img }}" style="height:28px;right:10px;left:auto;"/>
    {{ channel.name }}
  </li>
  {% for o in channel.epg if o.program.duration > 10 %}
    {% include 'SkreenHouseFactoryMobileBundle:Main:_program.html.twig' with {program: o.program, best_offer: o} %}
  {% endfor %}
</ul>
{% endfor %}

<script>
  UI.navbar('totv');
</script>
{% endblock %}